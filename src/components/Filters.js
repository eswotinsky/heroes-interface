import React from 'react';
import PropTypes from 'prop-types';

function Filters(props) {
  return (
    <div>
      <h1>Filters Placeholder</h1>
    </div>
  );
}

Filters.propTypes = {
  universe: PropTypes.string
};

export default Filters;
